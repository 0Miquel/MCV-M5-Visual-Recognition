n_epochs: 10
device: cuda
model_path: weights/first_model.pt  # path were the model will be saved
metrics: ["accuracy"]  # list of metrics to be computed and recorded every epoch
task: classification
grad_cam: True  # if True, will compute grad cam for the last batch of the last epoch