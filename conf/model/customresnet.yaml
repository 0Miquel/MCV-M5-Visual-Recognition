model_name: CustomResnet
settings:
  pretrained: False
  fine_tune: True  # if fine_tuning is False, then use transfer_learning (freeze all layers except output layer)
  n_classes: 8
  n_blocks: 2
  input_channels: 3
  channels: 8
  dropout_rate: 0.3